#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*numbers and char can be declared anywhere in between the code or at the start , doesn't matter
    even after declaring a variable we can change it any time*///Impotant
    char name[] = "Akhand Nachi";
    int age = 68;
    printf("There was once a man named %s\n", name);
    printf("He was Fuckin Virgin till the age of %d\n", age);

    age = 69;
    char names[] = "Cutiya";
    printf("HE really loved the name %s \n", names);
    printf("But he was so Horny till the age %d but not anymore ,cause he Died \n", age);
    printf("He Died a Virgin");

    return 0;
}
